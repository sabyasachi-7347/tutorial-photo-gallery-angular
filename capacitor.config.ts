import { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = {
  appId: "io.ssp.trukkerapp",
  appName: "Trukker App",
  bundledWebRuntime: false,
  npmClient: "npm",
  webDir: "www",
};

export default config;
